{
  "name": "code-template-documenter",
  "version": "1.0.0",
  "description": "",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "serve",
    "build": "esbuild script.js --bundle --outfile=dist/script.js --watch",
    "build2": "esbuild script-2.js --bundle --outfile=dist/script-2.js --watch",
    "build-by-file": "node builder.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.27.2",
    "serve": "^14.2.5"
  }
}
